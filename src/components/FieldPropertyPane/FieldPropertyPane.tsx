/**
 * @component
 * Renders the properties panel for configuring the currently selected field
 */
const FieldPropertyPane = () => {
    return (
        <aside className="w-72 h-full border-r border-border bg-surface">

        </aside>
    )
}

export default FieldPropertyPane